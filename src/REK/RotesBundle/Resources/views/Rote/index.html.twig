{% extends '::base.html.twig' %}

{% block stylesheets %}
{% endblock %}

{% block title %}ROTES{% endblock %}

{% block body %}

  {# <a class="back" href="{{ url('home') }}"><img src="{{ asset('bundles/rekrotes/images/back.png') }}" /></a> #}

  <div>
    {{ form_start(form) }}
        {{ form_errors(form) }}

        <div class="message">
            {{ form_errors(form.message) }}
            {{ form_widget(form.message) }}
        </div>

    {{ form_end(form) }}

  </div>

{% endblock %}